[Unit] 
Description=Stress test server{{ count }}

[Service] 
Type=forking 
ExecStart=/usr/sbin/daemonize -c /tmp -p /tmp/stressing{{ count }}.pid /usr/bin/stress -c 2 
PIDFile=/tmp/stressing{{ count }}.pid

[Install] 
WantedBy=multi-user.target

